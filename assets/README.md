# Assets

This directory contains redistributable demo media and application resources.

- `images/` - Application icons and UI graphics
- `video/` - Sample demo clips (keep small, < 10MB total)

Files here are tracked in git. For personal/large media, use `library/` instead.
